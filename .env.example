# Public environment variables (safe to commit)
# These are exposed to the browser and should never contain secrets

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# App Configuration
NEXT_PUBLIC_APP_NAME=Santria Dashboard
NEXT_PUBLIC_APP_VERSION=1.0.0

# Demo Mode (for public repository)
NEXT_PUBLIC_DEMO_MODE=true

# Optional: Analytics (if using in production)
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id

# Optional: Feature flags
# NEXT_PUBLIC_ENABLE_WHATSAPP_INTEGRATION=false
# NEXT_PUBLIC_ENABLE_REAL_DATA=false
